void loopStrategy() {
  switch (strategy) {
    
    case 0: // الحالة 000

      break;

    case 1: // الحالة 001

      break;

    case 2: // الحالة 010
      // ضع قيمك هنا
      break;

    case 3: // الحالة 011
      // ضع قيمك هنا
      break;

    case 4: // الحالة 100
      // ضع قيمك هنا
      break;

    case 5: // الحالة 101
      // ضع قيمك هنا
      break;

    case 6: // الحالة 110
      // ضع قيمك هنا
      break;

    case 7: // الحالة 111

      break;
  }
}
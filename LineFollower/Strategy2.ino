/*
      // بس يخلص الخط الاسود لف
      if (midSensor >= 7) {  // السير في خط مستقيم لتجاوز التقاطع
        caseMotor = 3;
        lineWasFound = false;
        lineAvailable = false;
        loopMotor();
        return;
      }

      // لازم افحص بت معين
      if (leftRadar == 2) {
    
      }
      */  
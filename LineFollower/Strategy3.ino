/*
      if (allSensor >= 11 || radar >= 3) {  // السير في خط مستقيم لتجاوز التقاطع
        caseMotor = 3;
        lineWasFound = false;
        lineAvailable = false;
        loopMotor();
        return;
      }

      // لازم افحص بت معين
      if (leftRadar == 2) {
    
      }
*/      